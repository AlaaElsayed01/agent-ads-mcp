services:
  - type: web
    name: agent-ads-mcp
    runtime: docker
    plan: starter
    envVars:
      - key: PORT
        value: "3000"
      # Add your ad platform tokens in Render dashboard > Environment
